spring:
  profiles:
    active: dev
  application:
    name: cyan-databi
  main:
    web-application-type: servlet
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
